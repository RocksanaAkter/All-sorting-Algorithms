
#include<stdio.h>

int main()
{
    int n, i, j, k, a[100];

    printf("How many elements u want to sort?");
    scanf("%d", &n);

    for(i=0; i<n; i++)
    {
        scanf("%d", &a[i]);
    }

    for(i=1; i<n; i++)
    {
        if(a[0]>a[i]);
        {
            b[i]=a[i];
        }
        else
            c[i]
    }


    return 0;
}
